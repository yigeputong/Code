{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "gcc 生成活动文件(Windows)",
            "command": "E:\\mingw64\\bin\\gcc.exe",
            "args": ["gcc -fdiagnostics-color=always ${file} -o ${fileDirname}\\${fileBasenameNoExtension}.exe -ggdb"],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"]
        },
        {
            "type": "cppbuild",
            "label": "gcc 生成活动文件(Linux)",
            "command": "/usr/bin/gcc",
            "args": ["gcc -fdiagnostics-color=always ${file} -o ${fileDirname}\\${fileBasenameNoExtension}.run -ggdb"],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"]
        },
        {
            "type": "cppbuild",
            "label": "gcc 生成活动文件(Windows, 优化)",
            "command": "E:\\mingw64\\bin\\gcc.exe",
            "args": ["gcc -fdiagnostics-color=always ${file} -o ${fileDirname}\\${fileBasenameNoExtension}.exe -ggdb -O3"],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"]
        },
        {
            "type": "cppbuild",
            "label": "gcc 生成活动文件(Linux, 优化)",
            "command": "/usr/bin/gcc",
            "args": ["gcc -fdiagnostics-color=always ${file} -o ${fileDirname}\\${fileBasenameNoExtension}.run -ggdb -O3"],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$gcc"]
        }
    ],
    "version": "2.0.0"
}